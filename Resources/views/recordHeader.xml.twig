<header>
  <identifier>{{ get_record_id(record) }}</identifier>
  <datestamp>{{ get_record_updated(record)|date('Y-m-d') }}</datestamp>
  {% for setName in get_record_sets(record) %}
    <setSpec>{{ setName }}</setSpec>
  {% endfor %}
</header>
