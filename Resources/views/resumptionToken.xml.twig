
<resumptionToken
  {% if resumption %}expirationDate="{{ resumption.expiresOn|date('Y-m-d\\TH:i:sP') }}"{% endif %}
  completeListSize="{{ totalCount }}"
  cursor="{{ starts }}"
>{% if resumption %}{{ resumption.token }}{% endif %}</resumptionToken>
